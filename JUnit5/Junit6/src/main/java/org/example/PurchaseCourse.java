package org.example;

public class PurchaseCourse {

    public boolean proceedWithCourse(Course course){// eg Course course=new JavaCourse(); means instance of javaCourse class will be called
        return course.coursePurchase();
    }
}
